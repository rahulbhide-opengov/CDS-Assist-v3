/**
 * Permitting navigation configuration.
 */

import type { NavConfig } from '../../types';

/** Permitting nav config. */
export const permittingNavConfig: NavConfig = {
  appName: 'Permitting',
  menuOptions: [{ id: 'dashboard', label: 'Dashboard', path: '/permitting' }],
};
