/**
 * Financials navigation configuration.
 */

import type { NavConfig } from '../../types';

/** Financials nav config. */
export const financialsNavConfig: NavConfig = {
  appName: 'Financials',
  menuOptions: [{ id: 'dashboard', label: 'Dashboard', path: '/financials/dashboard' }],
};
