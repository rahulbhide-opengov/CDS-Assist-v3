/**
 * Budgeting navigation configuration.
 */

import type { NavConfig } from '../../types';

/** Budgeting nav config. */
export const budgetingNavConfig: NavConfig = {
  appName: 'Budgeting',
  menuOptions: [{ id: 'dashboard', label: 'Dashboard', path: '/budgeting' }],
};
